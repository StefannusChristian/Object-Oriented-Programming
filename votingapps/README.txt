How to Run the Program:
python -m my_module -dbinit=false --> run the program with old database
python -m my_module -dbinit=true --> run the program with fresh new database

Database Requirements:
To run this program, you will need sqlite3 installed on your OS.
You can see the tables in the excel given above.

To install sqlite3, you can google it.